
language: node_js
node_js:
  - node
  - lts/*
before_script:
  - cd mobile && yarn
script:
  - cd mobile
  - yarn add expo-cli
  - yarn expo login -u $EXPO_USERNAME -p $EXPO_PASSWORD
  - yarn expo publish --non-interactive